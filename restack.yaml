name: csm-agent
description: Customer Service Management Agent
runtime: python3.9

build:
  commands:
    - pip install -r requirements.txt

run:
  command: uvicorn csm-agent:app --host 0.0.0.0 --port $PORT

env:
  - name: PORT
    value: "8000" 